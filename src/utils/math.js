 const add = (a, b) =>  a + b;
 const sub = (a, b) =>  a - b;
 const mul = (a, b) =>  a * b;
 const div = (a, b) =>  a / b;

const obj = {
    add: add,
    sub: sub,
    mul: mul,
    div: div
}
export const arr = [2, 10, 22, 3, 5, 17, 4, 6]
export default obj